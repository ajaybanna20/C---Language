/* Q)write a c program input character and check character is vowel and consonant ?
vowel :a,e,i,o,u consoant : */
#include <stdio.h>
int main () {
	char ch;
	printf("enter a character: ");
	scanf("%c",&ch);
	if (ch =='a') {
		printf("vowel");
	}
	else if (ch == 'e') {
		printf("vowel");
	}
	else if (ch == 'i') {
		printf("vowel");
	} else if (ch == 'o') {
		printf("vowel");
	}
	else if (ch == 'u') {
		printf("vowel");
	}
	else {
		printf("consonent");
	}
printf("\nrest of the work");
return 0;
}
